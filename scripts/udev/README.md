# Host udev rules

Operator doc: [`docs/UDEV_HOST_SETUP.md`](../../docs/UDEV_HOST_SETUP.md)

```bash
sudo bash scripts/udev/install.sh
```

Installs every `rules.d/*.rules` into `/etc/udev/rules.d/`.
