# Contributing to execution_manager

We welcome contributions! Please ensure:
1. All C++ code follows the established coding style and passes clang-format / cpplint.
2. Unit tests in `test/` pass without errors (`colcon test`).
3. Wire contracts remain compatible with `execution_manager_interfaces`.
